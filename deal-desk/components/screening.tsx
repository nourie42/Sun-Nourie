import {Button} from './ui/button';
import {Input} from './ui/input';
import {money,fields} from '../lib/deal';
import {synergyRows,synergyOptions,channelResults,purchaseRecommendation} from '../lib/screening.js';
export function PurchaseScreen({deal,result,update,rerun}:any){const p=purchaseRecommendation(deal,result);return <section className="panel purchase-screen"><p className="eyebrow">ESTIMATED PURCHASE PRICE · {deal.name}</p><h2>{p.value===null?'Upload files or search a company':money(p.value)}</h2><p>Estimated opening recommendation · asset / enterprise consideration, not equity market value</p>{p.value!==null&&<><div className="double-kpi"><div><strong>{money(p.low)} – {money(p.high)}</strong><p>Screening range</p></div><div><strong>{money(p.capacity)}</strong><p>Ceiling at {deal.hurdle??15}% pretax return</p></div></div><p>{p.basis}</p><p>Current modeled consideration: <strong>{money(deal.price)}</strong>. This is a screening estimate, not an appraisal or a seller quote.</p><label>Terminal recovery (% of recommended consideration)<Input type="number" min="0" max="100" value={deal.exitRecovery??80} onChange={e=>update('exitRecovery',Number(e.target.value))}/></label><div className="actions"><Button onClick={()=>{update('price',p.value);update('terminal',p.value*(deal.exitRecovery??80)/100);}}>Use recommended price</Button><Button variant="outline" onClick={rerun}>Rerun economics</Button></div></>}</section>}
export function SynergyScreen({deal,update,rerun}:any){const rows=synergyRows(deal);return <><section className="panel"><h2>Synergies included and excluded</h2><p>Every nonzero amount below is already reflected in the economics. Zero rows are excluded. Rent and commission transfer earnings between Sunoco and the dealer; they are not combined synergies.</p><div className="synergy-list">{rows.map((x:any,i:number)=><article key={i} className="notice"><h3>{x.name}</h3><strong>{x.included?'Included':'Not included'} · {money(x.amount)}</strong><p>{x.scope} · {x.basis}</p></article>)}</div><h3>Edit recurring synergies</h3><p>Additional savings are net of implementation costs and must not duplicate reductions already entered in retained Opex or G&A. Dealer inside profit benefits the dealer, not Sunoco.</p><div className="field-grid">{synergyOptions.map(([key,label,unit,scope]:string[])=><label className="field" key={key}>{label}<small>{scope} · {unit}</small><Input aria-label={label} type="number" step="any" value={deal[key]??0} onChange={e=>update(key,Number(e.target.value))}/></label>)}<label className="field">Eligible retail gallons (%)<Input aria-label="Eligible retail gallons percent" type="number" value={deal.eligible??0} onChange={e=>update('eligible',Number(e.target.value))}/></label></div><h3>Costs and dealer terms already in the conversion bridge</h3><div className="field-grid">{fields.filter(f=>['sellerGa','retainedGa','card','property','lease','maintenanceOpex','retainedOther','dealerOpex','rent','commission'].includes(f.key)).map(f=><label className="field" key={f.key}>{f.label}<small>{f.unit}</small><Input aria-label={'Synergy basis '+f.label} type="number" step="any" value={deal[f.key]??0} onChange={e=>update(f.key,Number(e.target.value))}/><small>{f.note}</small></label>)}</div><Button onClick={rerun}>Rerun economics</Button></section><ChannelScreen deal={deal} update={update}/></>}
export function ChannelScreen({deal,update}:any){const channels=channelResults(deal);const change=(i:number,key:string,value:any)=>update('channels',(deal.channels||[]).map((c:any,j:number)=>i===j?{...c,[key]:value,metricEvidence:{...c.metricEvidence,[key]:{status:'User-entered',reason:'Edited by user'}}}:c));return <section className="panel"><h2>Operating channels and footprint</h2><p>Company-operated retail: <strong>{deal.sites??'Unknown'} sites</strong>. These are the sites used for retail dealer EBITDA per site. Existing dealers and wholesale customers retain supply economics and do not receive retail store-conversion costs.</p><p>Reported footprint counts are separate from individual address records. Company-operated does not mean the real estate is owned.</p>{channels.map((c:any,i:number)=><details key={i} open><summary>{c.name} · {c.sites.toLocaleString()} locations · {money(c.ebitda)} annual EBITDA</summary><p>{c.basis}</p><div className="field-grid">{[['sites','Locations'],['gallons','Annual gallons'],['fuelCpg','Supply margin (cents/gallon)'],['other','Other gross profit / rent'],['opex','Annual cash operating costs'],['ga','Annual allocated G&A'],['capex','Annual maintenance capex'],['procurement','Purchasing improvement (cents/gallon)'],['eligible','Eligible volume (%)'],['savings','Additional recurring savings']].map(([key,label])=><label className="field" key={key}>{label}<Input aria-label={c.name+' '+label} type="number" step="any" value={c[key]} onChange={e=>change(i,key,Number(e.target.value))}/><small>{c.metricEvidence?.[key]?.status||'Estimated / user assumption'}{c.metricEvidence?.[key]?.quote?' · '+c.metricEvidence[key].quote:''}</small><small>{c.metricEvidence?.[key]?.reason}</small></label>)}</div><p>Baseline EBITDA {money(c.baseline)} + purchasing {money(c.supply)} + additional savings {money(c.savings)} = {money(c.ebitda)}. Maintenance capex {money(c.capex)} is deducted from cash flow.</p><Button variant="outline" onClick={()=>update('channels',deal.channels.filter((_:any,j:number)=>j!==i))}>Remove {c.name}</Button></details>)}<Button variant="outline" onClick={()=>update('channels',[...(deal.channels||[]),{name:'Additional dealer / wholesale '+(channels.length+1),type:'dealer',sites:0,gallons:0,fuelCpg:0,other:0,opex:0,ga:0,capex:0,procurement:0,eligible:0,savings:0,basis:'User-added channel. Enter only this channel’s figures; avoid overlap with existing channels.',status:'User-entered'}])}>Add dealer / wholesale channel</Button></section>}
