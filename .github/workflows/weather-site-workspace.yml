name: Weather site test workspace
on:
  push:
    branches: [weather/plain-nav-weathernext-site]
    paths: ['.github/workflows/weather-site-workspace.yml']
permissions:
  contents: read
jobs:
  workspace:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm ci --ignore-scripts
      - name: Package tracked source and test dependencies
        run: |
          mkdir -p /tmp/weather-workspace
          git archive HEAD | tar -x -C /tmp/weather-workspace
          cp -a node_modules /tmp/weather-workspace/node_modules
          mkdir -p /tmp/weather-workspace/qa
          curl -fsSL https://raw.githubusercontent.com/nourie42/Sun-Nourie/weather-fusion-data/models/weathernext3.json -o /tmp/weather-workspace/qa/weathernext-live.json
          tar -czf /tmp/weather-workspace.tar.gz -C /tmp/weather-workspace .
      - uses: actions/upload-artifact@v4
        with:
          name: weather-site-workspace
          path: /tmp/weather-workspace.tar.gz
          retention-days: 1
